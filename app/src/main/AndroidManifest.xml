<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.google.android.gms.nearby.messages.samples.nearbydevices">

    <!-- Required for Nearby Connections -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="com.google.android.nearby.messages.API_KEY"
            android:value="AIzaSyDitsJAmiLA1PAi-QpduV0siE4IoL0kA2o" />

        <activity android:name=".connections_activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:label="@string/app_name"
            android:name=".start_poll_activity"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:label="@string/app_name"
            android:name=".get_poll_activity"
            android:windowSoftInputMode="adjustPan" />

        <activity
            android:label="Nearby Polls"
            android:name=".start_poll_2_activity"
            android:windowSoftInputMode="adjustResize"/>

        <activity
            android:label="Nearby Polls"
            android:name=".get_poll_2_activity"
            android:windowSoftInputMode="adjustResize"/>
    </application>

</manifest>
